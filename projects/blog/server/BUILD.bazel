load(
    "@rules_haskell//haskell:defs.bzl",
    "haskell_binary",
)

haskell_binary(
    name = "server",
    srcs = ["src/Main.hs"],
    deps = [
        "@stackage//:base",
        "@stackage//:scotty",
    ],
)
